<%= form_for (@image) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :image %>
  <%= f.file_field :image %>
  <%= f.hidden_field(:user_id, :value => current_user.id) %>
  <%= f.submit %>
<% end %>

<p> <%= link_to "Return to all images", images_path  %></p>
