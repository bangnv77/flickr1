<% if @images.any? %>
  <ul>
    <% @images.each do |image| %>
      <li>
        <div class="image-display">
          <%= link_to (image_tag image.image.url(:medium)), image_path(image) %>
        </div>
      </li>
    <% end %>
  </ul>
<% else %>
  <p> No images yet. </p>
<% end %>
